# Storybook 系统概述

> 项目代号：storybook | 模块：`git.neolidy.top/neo/storybook` | Go 版本：1.25.4

## 一、项目定位

Storybook 是一个围绕用户故事（User Story）的敏捷协作系统，覆盖产品、开发、测试、技术负责人、管理员五类角色。当前仓库已经不止于 MVP，而是包含项目管理、故事审批、任务拆分、测试用例、缺陷、报表、全局搜索、MCP 接口与嵌入式前端。

核心流程如下：

1. 产品经理在项目下创建故事，初始状态为 `pending`，等待审批。
2. 技术负责人或管理员审批故事；通过后进入 `backlog`，驳回则保留审批意见。
3. 产品经理、技术负责人或管理员可将故事分配给项目内开发成员。
4. 开发、测试围绕故事继续推进任务、测试用例、缺陷与状态流转。

## 二、当前技术架构

### 2.1 后端

- 框架：Gin
- 数据访问：GORM
- 数据库：SQLite（默认）/ PostgreSQL（可选）
- 认证：JWT Access Token + Refresh Token
- 实时能力：gorilla/websocket
- 配置方式：环境变量读取，入口在 `internal/config`
- 接口组织：`handler -> service -> repository/model`

### 2.2 前端

- React 19 + TypeScript
- 路由：`react-router-dom` 7.x
- 状态管理：Zustand
- 构建工具：Rsbuild
- 样式：Tailwind CSS 4
- 测试：Vitest + Testing Library

### 2.3 前后端集成

前端源码位于 `storybook-page/`。生产构建后可同步到 `internal/webui/dist`，由后端通过 `embed` 提供静态资源与 SPA 回退路由。仓库同时保留：

- `pnpm run build:embed`：前端侧直接复制构建产物
- `go run ./cmd/embedui`：后端侧同步构建产物

## 三、当前能力边界

仓库已实现的主干能力包括：

- 用户注册、登录、刷新令牌、登录失败锁定
- 项目 CRUD、成员管理、技术负责人管理、概览统计
- 用户故事 CRUD、看板、审批、分配、归档、活动历史、代码引用
- 子任务、测试用例、冲刺、缺陷管理
- 速度、质量、燃尽报表
- 全局搜索、个人工作台、WebSocket 推送
- MCP HTTP 接口：故事 AC、覆盖率、状态更新、生成 AC 测试模板、代码与 AC 分析
- 用户管理接口：列表、创建、更新、工作负载、删除

## 四、目录结构

```text
storybook/
├── cmd/
│   ├── server/        # 主 HTTP 服务
│   ├── mcp/           # MCP 服务入口
│   └── embedui/       # 同步前端产物到嵌入目录
├── internal/
│   ├── api/           # 统一响应封装
│   ├── auth/          # JWT 与鉴权
│   ├── config/        # 环境变量配置
│   ├── database/      # 数据库连接与 AutoMigrate
│   ├── e2e/           # 端到端测试
│   ├── handler/       # HTTP 处理器
│   ├── middleware/    # 认证、限流、CORS
│   ├── model/         # GORM 模型与常量
│   ├── realtime/      # WebSocket Hub
│   ├── router/        # 路由注册
│   └── webui/         # embed 前端静态资源
├── pkg/mcp/           # 可复用 MCP 服务实现
├── storybook-page/    # React 前端
├── docs/              # 设计与接口文档
└── scripts/           # 辅助脚本
```

## 五、运行与配置

默认配置来自环境变量：

- `SERVER_ADDR`：默认 `:8080`
- `DB_DRIVER`：默认 `sqlite`
- `DB_DSN`：默认 `storybook.db`
- `JWT_SECRET`：生产环境必须覆盖

数据库初始化由应用启动时的 `AutoMigrate` 完成，文档中的 SQL、触发器和备份策略应视为设计建议，不代表当前仓库已经全部落地。

## 六、相关文档

- [02-用户故事拆解.md](./02-用户故事拆解.md)
- [03-数据库设计.md](./03-数据库设计.md)
- [04-API设计.md](./04-API设计.md)
- [05-MCP服务设计.md](./05-MCP服务设计.md)
- [06-openapi.yaml](./06-openapi.yaml)
- [07-前端实施计划.md](./07-前端实施计划.md)
- [08-技术负责人角色实施计划.md](./08-技术负责人角色实施计划.md)
